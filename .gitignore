node_modules/
dist/
coverage/
.env
.env.local
*.log
.DS_Store
.vscode/
.idea/

# Acquired client codebases and their subject files (OGE-2425).
# Client code stays on the machine and is NEVER committed. `audit acquire`
# defaults here; an explicit --into elsewhere is the operator's responsibility.
work/
*.subject.json
